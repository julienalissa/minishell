prob :

1. ne pas affichier minishell > mais les path (option)
2. ctrl + c et apres pritn $? doit etre 130 et c'est 0
3. unset PATH et apres ls pas de message d'erreur
4. problemes avec tout les redirections
5. ~/le lien clea ne marche pas par exmple cat ~/test.txt
6. echo $PWD ne marche pas (si on fait un $PWD et apres on change d'endroit cela affiche le meme pwd, meme chose pour $OLDPWD)
7. si on fait une mauvaise commande et apres on ctrl + d le code d'exite n'est pas bon cela ne garde pas le code d'erreur
8. si il ya un script dans un fichier un .sh par exmple et que on le lance, cela ne echo pour le resultat si on le lance (./file.sh)
9. norminette des trucs
10. les leaks sont pas bon chef
